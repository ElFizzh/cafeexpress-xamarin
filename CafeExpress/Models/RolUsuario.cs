namespace CafeExpress.Models
{
    /// <summary>Roles disponibles en el sistema (modelo RBAC).</summary>
    public enum RolUsuario
    {
        Administrador,
        Empleado,
        Cliente
    }
}
