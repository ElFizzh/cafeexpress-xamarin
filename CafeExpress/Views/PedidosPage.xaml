<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CafeExpress.ViewModels"
             x:Class="CafeExpress.Views.PedidosPage"
             Title="{Binding Titulo}"
             BackgroundColor="{StaticResource ColorFondo}">

    <ContentPage.BindingContext>
        <vm:PedidosViewModel />
    </ContentPage.BindingContext>

    <ListView ItemsSource="{Binding Pedidos}"
              HasUnevenRows="True"
              SelectionMode="None"
              Margin="8">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Frame Margin="6" Padding="10" CornerRadius="8" HasShadow="False"
                           BorderColor="#E0D5C8">
                        <StackLayout Spacing="4">
                            <Label Text="{Binding Descripcion}" FontAttributes="Bold" FontSize="14" />
                            <Label Text="{Binding Resumen}" FontSize="12" TextColor="Gray" />
                            <Label Text="{Binding EstadoTexto, StringFormat='Estado: {0}'}"
                                   FontSize="13"
                                   TextColor="{StaticResource ColorAcento}" />

                            <!-- Visible solo para Empleado y Administrador (matriz RBAC) -->
                            <Button Text="Avanzar estado"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PedidosViewModel}}, Path=AvanzarEstadoCommand}"
                                    CommandParameter="{Binding .}"
                                    IsVisible="{Binding Source={RelativeSource AncestorType={x:Type vm:PedidosViewModel}}, Path=PuedeActualizarEstado}" />
                        </StackLayout>
                    </Frame>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
